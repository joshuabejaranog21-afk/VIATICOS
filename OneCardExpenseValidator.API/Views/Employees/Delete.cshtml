@model OneCardExpenseValidator.Infrastructure.Entities.Employee

@{
    ViewData["Title"] = "Desactivar Empleado";
}

<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
    <h1 class="h2 text-danger"><i class="bi bi-exclamation-triangle"></i> Desactivar Empleado</h1>
    <div class="btn-toolbar mb-2 mb-md-0">
        <a asp-action="Index" class="btn btn-secondary">
            <i class="bi bi-arrow-left"></i> Volver a la Lista
        </a>
    </div>
</div>

<div class="alert alert-warning" role="alert">
    <h4 class="alert-heading"><i class="bi bi-info-circle"></i> Confirmación requerida</h4>
    <p>¿Está seguro de que desea desactivar este empleado? Esta acción marcará al empleado como inactivo pero no eliminará su información.</p>
</div>

<div class="row">
    <div class="col-md-8">
        <div class="card shadow">
            <div class="card-header bg-light">
                <h5 class="mb-0">Información del Empleado</h5>
            </div>
            <div class="card-body">
                <dl class="row">
                    <dt class="col-sm-4">Código de Empleado:</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.EmployeeCode)</dd>

                    <dt class="col-sm-4">Nombre Completo:</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.FirstName) @Html.DisplayFor(model => model.LastName)</dd>

                    <dt class="col-sm-4">Email:</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.Email)</dd>

                    <dt class="col-sm-4">Departamento:</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.Department.DepartmentName)</dd>

                    <dt class="col-sm-4">Puesto:</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.Position)</dd>

                    <dt class="col-sm-4">Límite Diario:</dt>
                    <dd class="col-sm-8">@(Model.DailyExpenseLimit?.ToString("C") ?? "N/A")</dd>

                    <dt class="col-sm-4">Límite Mensual:</dt>
                    <dd class="col-sm-8">@(Model.MonthlyExpenseLimit?.ToString("C") ?? "N/A")</dd>

                    <dt class="col-sm-4">Estado Actual:</dt>
                    <dd class="col-sm-8">
                        @if (Model.IsActive == true)
                        {
                            <span class="badge bg-success">Activo</span>
                        }
                        else
                        {
                            <span class="badge bg-secondary">Inactivo</span>
                        }
                    </dd>
                </dl>

                <form asp-action="Delete" class="mt-4">
                    <input type="hidden" asp-for="EmployeeId" />
                    <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                        <button type="submit" class="btn btn-danger">
                            <i class="bi bi-x-circle"></i> Confirmar Desactivación
                        </button>
                        <a asp-action="Index" class="btn btn-secondary">
                            Cancelar
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
